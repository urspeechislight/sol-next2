{
  "mcpServers": {
    "sol-next2-corpus": {
      "command": "uv",
      "args": ["run", "--project", "${workspaceFolder}", "python", "-m", "mcp_server.server"],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  }
}
