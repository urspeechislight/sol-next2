# sol-next configuration
# All registries and thresholds in one file.
# Validation at startup catches every broken cross-reference.

# ─────────────────────────────────────────────────────────────────────────────
# PATTERNS
# Surface-level text detectors. Phase 2 runs these against span text.
# Each match becomes a Pattern object on the span.
# ─────────────────────────────────────────────────────────────────────────────
patterns:
  - id: ATTRIBUTION
    description: >
      Hadith transmission verbs and markers (حدثنا, أخبرنا, عن, روى).
      Includes common abbreviations: أنا (=أخبرنا), نا (=حدثنا).
      These abbreviations are extremely common in classical hadith collections
      (شرح السنة, المعجم الأوسط, etc.). False matches of أنا as pronoun "I"
      are filtered by _filter_false_attributions() using pronoun_ana_exclusions.

      start_threshold restricts ROUTING relevance to the first 120 chars of
      a span — a hadith chain has the transmission verb near the entry start,
      not buried hundreds of chars into narrative. Boundary detection
      (is_boundary) is unaffected by start_threshold.
    regex: "\\bو?(?:حدثنا|حدثني|ثنا|أنا|نا|أخبرنا|أخبرني|روى|روينا|أنبأنا|نبأنا|سمعت|سمعنا|بإسناده)\\b|وعن\\b|\\bعن\\b"
    is_boundary: true
    start_threshold: 120

  - id: ATTRIBUTION_STRONG
    description: >
      Unambiguous hadith transmission verbs only. Unlike ATTRIBUTION, this
      excludes the polysemous عن which can mean "about" in non-chain contexts.
      Used for behavior routing; ATTRIBUTION (with عن) remains for boundaries.
    regex: "\\bو?(?:حدثنا|حدثني|ثنا|أخبرنا|أخبرني|روى|روينا|أنبأنا|نبأنا|سمعت|سمعنا|بإسناده)\\b"

  - id: ATTRIBUTION_STRONG_INITIAL
    description: >
      Strong-attribution verb at the START of a span — a real hadith chain
      indicator, as opposed to a mid-text citation. A modern sīrah author
      writing "...فقد روى ابن هشام عن..." mid-paragraph (citation of a
      classical work) makes plain ATTRIBUTION_STRONG fire and triggers
      HADITH_TRANSMISSION variant 1 (priority 80), which is the wrong
      classification — the surrounding span is narrative, not a hadith.

      This pattern fires only when the strong-attribution verb sits within
      the first few chars of the span (after optional numbered/decorative
      prefix). Hadith collection entries — ``1 - حدثنا أبو بكر...``,
      ``حدثنا ابن إسحاق``, ``روى البخاري عن...`` — match. Narrative spans
      with mid-text citations do not.
    regex: "^[\\s(*0-9٠-٩\\-.\\)]*و?(?:حدثنا|حدثني|ثنا|أخبرنا|أخبرني|روى|روينا|أنبأنا|نبأنا|سمعت|سمعنا|بإسناده)\\b"
    start_threshold: 20

  - id: SPEECH_VERB_GENERIC
    description: >
      Generic speech verb (قال, يقول, قيل). Anchored with non-Arabic-letter
      lookarounds — Python's \b is ASCII-aware and is a no-op inside Arabic
      text, so unanchored stems would match as substrings of unrelated tokens
      (e.g. قال inside مقالات). The optional [وفبلك]?(?:ال)? prefix permits common
      Arabic clitics (conjunction و/ف, prepositions ب/ل/ك, definite article ال)
      so وقال and فيقول still match.
    regex: "(?<![؀-ۿ])[وفبلك]?(?:ال)?(?:قال|قالوا|قالت|يقول|قيل|ذكر|قلت)(?![؀-ۿ])"

  - id: MATN_BOUNDARY_HINT
    description: >
      Prophet attribution phrases that mark matn start. Used by Phase 3 for
      sanad/matn splitting, NOT for behavior routing. These phrases identify
      where the matn begins within a hadith — they do not indicate that a
      hadith chain exists.
    regex: "(?:قال|عن|أن|كان|أمر|نهى|سئل|رأيت)\\s+(?:رسول الله|النبي)|مرفوعا|رفعه|يرفعه|يبلغ به"

  - id: QURAN_REF
    description: Quranic quotation markers
    regex: "قال الله تعالى|قال تعالى|قوله تعالى|قوله عز وجل|﴿|\\bسورة"

  - id: GENEALOGY_CHAIN
    description: >
      Genealogy/isnad chain marker (ابن, بن, بنت). start_threshold restricts
      ROUTING relevance to the first 120 chars — a hadith chain's first
      narrator name appears at the very start of the entry, while a cited
      name buried in mid-narrative is incidental. Detection itself is
      unaffected by start_threshold (only the routing rules see the limit).
    regex: "\\bبن\\b|\\bابن\\b|\\bبنت\\b"
    start_threshold: 120

  - id: DEATH_MARKER
    description: >
      Death date marker (توفي, مات, وفاته). Anchored with non-Arabic-letter
      lookarounds so مات doesn't match inside قامات/علامات/أمات, etc. Optional
      [وفبلك]?(?:ال)? prefix permits Arabic clitics (e.g. وتوفي, فمات).
    regex: "(?<![؀-ۿ])[وفبلك]?(?:ال)?(?:توفي|توفيت|مات|ماتت|وفاته|وفاتها|المتوفى|المتوفاة)(?![؀-ۿ])"

  - id: BIRTH_MARKER
    description: >
      Birth date marker (ولد, مولده). Anchored so ولد doesn't match inside
      الولدان/أولاد/مولود. Optional [وفبلك]?(?:ال)? prefix permits Arabic clitics.
    regex: "(?<![؀-ۿ])[وفبلك]?(?:ال)?(?:ولد|ولدت|مولده|مولدها|نشأ)(?![؀-ۿ])"

  - id: PERSON_REF
    description: Person reference markers (الشيخ, الإمام, العالم)
    regex: "(?:الشيخ|الإمام|العالم|الحافظ|المحدث|الفقيه|القاضي|الأمير|الحاكم)\\b"

  - id: LAQAB_MARKER
    description: >
      Explicit laqab/epithet introduction (المعروف بـ, الملقب بـ).
      Phase 2 detection, consumed by Phase 3 extractors. Not for behavior routing.
    regex: "المعروف\\s+ب|يعرف\\s+ب|الملقب\\s+ب|المشهور\\s+ب"

  - id: FIQH_RULING
    description: >
      Legal ruling markers (يجب, يحرم, يستحب). Anchored with non-Arabic-letter
      lookarounds — without them يجب matches the prefix of يجبنوا ("they shrink
      back", root ج-ب-ن), فرض matches فرضية, etc. Optional [وفبلك]?(?:ال)?
      prefix permits Arabic clitics (الواجب, ويجب, فالواجب).

      Bare adjective حرام is intentionally NOT in this alternation: in
      sīrah/tarīkh genres it is a proper-noun attribute (البيت الحرام,
      المسجد الحرام, الشهر الحرام), and disambiguating per-clitic with
      Python's fixed-width lookbehinds is impractical. Real fiqh statements
      ("هذا حرام") are typically corroborated by the verbal forms يحرم /
      مكروه / يجب that remain in the alternation.
    regex: "(?<![؀-ۿ])[وفبلك]?(?:ال)?(?:يجب|يحرم|يكره|يستحب|يباح|فرض|واجب|مكروه|مندوب|مباح)(?![؀-ۿ])"

  - id: BASMALA
    description: Basmala (بسم الله الرحمن الرحيم)
    regex: "بسم\\s+الله\\s+الرحمن\\s+الرحيم"
    is_boundary: true

  - id: HEADING_MARKER
    description: >
      Structural heading keywords across genres, with optional ال prefix,
      decorative wrappers, and continuation prefixes (تتمة, بقية) for
      volumes that begin mid-chapter.
    regex: "^[\\s(*0-9٠-٩]*(?:(?:تتمة|بقية)\\s+)?(?:ال)?(?:كتاب|باب|فصل|فصول|مسألة|مسائل|مسند|قسم|مقصد|مبحث|مطلب|حرف|سورة|قول في|كلام في)(?=[\\s*()\\n]|$)"
    is_boundary: true
    heading_stop_patterns: ["الآيات"]
    heading_disqualifiers:
      - "\\s+فف"
      - "\\s+و[إأ]ن"
      - "\\s+(?!ال)\\S{2,}(?:هم|هما|هن|ها)\\b"
      - "\\s+(?:على|إلى|من|عن|مع|لدى|نحو)\\b"
    hierarchy_levels:
      kitab: ["كتاب", "مسند", "حرف", "سورة", "قسم"]
      bab: ["باب", "مسألة", "مسائل", "القول في", "الكلام في", "قول في", "كلام في", "مقصد", "مبحث"]
      fasl: ["فصل", "فصول", "مطلب"]

  - id: AUTHOR_COMMENTARY
    description: Author commentary markers (بيان, أقول)
    regex: "\\bبيان\\s*:|\\bأقول\\b|\\bتفسير\\s*:"
    is_boundary: true
    start_threshold: 20

  - id: HIJRI_YEAR
    description: >
      Hijri year mention in narrative. Common shapes:
        سنة 200 هـ      ("year 200 AH")
        في سنة 5 هـ    ("in year 5 AH")
        عام 150        ("year 150")
        (ت 150)        ("died 150" — short form)
        (150 هـ)       ("(150 AH)")
      Western digits and Arabic-Indic digits both supported.
    regex: "(?:\\bسنة\\s+|\\bعام\\s+|\\bفي\\s+سنة\\s+|\\bعام\\s+|\\bت\\s+)[٠-٩0-9]{1,4}(?:\\s*ه[ـ]?\\b|\\s*هجر\\S{0,5}|\\b)"

  - id: LUNAR_MONTH
    description: >
      Names of the twelve Islamic lunar months. Captured for narrative-date
      anchoring (events placed in شهر رمضان من العام ..., ذي الحجة, etc.).
      Phase 3 emits LUNAR_MONTH entities for each match.
    regex: "\\b(?:محرم|صفر|ربيع\\s+الأول|ربيع\\s+الآخر|ربيع\\s+الثاني|جمادى\\s+الأولى|جمادى\\s+الآخرة|جمادى\\s+الثانية|رجب|شعبان|رمضان|شوال|ذو\\s+القعدة|ذي\\s+القعدة|ذو\\s+الحجة|ذي\\s+الحجة)\\b"

  - id: CLASSICAL_SOURCE_ATTRIBUTION
    description: >
      Deterministic citation marker — an attribution verb (قال/يقول/ذكر/روى/أخرج)
      followed by a recognised classical-source author or work. Captured as the
      surface trigger; the citation_extractor turns it into a CITATION_REF entity
      whose text is the cited source. This is the per-claim asserter signal the
      council identified as the deterministic backbone of provenance — no LLM
      required when the author writes "قال ابن هشام".
    regex: "(?:قال|يقول|ذكر|روى|أخرج|نقل|أخبر|يحكي|يقص|يروي)\\s+(?:ابن\\s+هشام|ابن\\s+إسحاق|ابن\\s+سعد|ابن\\s+كثير|ابن\\s+حجر|ابن\\s+الأثير|ابن\\s+الجوزي|ابن\\s+قتيبة|ابن\\s+عبد\\s+ربه|الطبري|الواقدي|البيهقي|السيوطي|الذهبي|البخاري|مسلم|أبو\\s+داود|الترمذي|النسائي|ابن\\s+ماجه|أحمد|الزهري|الكليني|الصدوق|الطوسي|المفيد|المجلسي)"

  - id: JARH_TADIL
    description: Reliability assessment terminology (narrator grading terms)
    regex: "\\b(?:ثقة|ثقات|صدوق|ضعيف|ضعفوه|متروك|كذاب|مجهول|لا بأس به|ليس بثقة|حسن الحديث|صالح|منكر|واه|حجة|ثبت|متقن|لين|صالح الحديث)\\b"

  - id: ISNAD_BACK_REF
    description: Isnad back-reference — author references previous hadith's chain
    regex: "(?:و)?بهذا\\s+ال[اإ]سناد|بال[اإ]سناد\\s+(?:عنه|المتقدم|السابق)"
    start_threshold: 30

  - id: AN_CHAIN_INITIAL
    description: >
      Chain-initial عن — isnad begins with عن rather than an explicit
      transmission verb. Requires span-start anchoring via start_threshold
      to avoid matching the common prepositional عن ("about") mid-span.

      A narrator-chain عن is always followed by a name (عن مالك, عن أبي هريرة,
      عن ابن عباس, عن الأعمش). The negative lookahead excludes the closed
      grammatical classes that follow prepositional عن but are never narrator
      names: demonstratives (عن هذا الأخير = "from this latter one"), relative
      pronouns (عن الذي), and place adverbs (عن هنا). This is what keeps an
      editor's introductory prose — which discusses transmission using عن هذا /
      عن ذلك and cites narrator names elsewhere in the sentence — from being
      classified as a hadith chain and chain-walked into non-name fragments.
    regex: "^\\s*و?عن\\s+(?!(?:هذا|هذه|هذان|هذين|هاتان|هاتين|هؤلاء|ذلك|ذلكم|تلك|أولئك|هنا|هناك|الذي|التي|الذين|اللذين|اللاتي|اللواتي)(?![؀-ۿ]))"
    start_threshold: 5

  - id: NUMBERED_ENTRY
    description: >
      Numbered list entries (1., 1 -, ١., (1), ١), etc.). Accepts both
      suffix forms (``5.`` / ``5 -`` / ``5)``) and parenthesized form
      (``(5)`` / ``(٦٨٩)``) — the latter is common in indexed rijal works
      like al-Ajli's Maʿrifat al-Thiqāt where each narrator entry is
      prefixed ``(N)``. Negative lookahead ``(?!\s*[0-9٠-٩])`` blocks
      number-range shapes like ``23 - 26`` (Qurʾān verse-range refs:
      ``الأعراف: 23-26``) from being treated as list-item starts.
    regex: "^\\s*\\*?\\s*\\(?[١٢٣٤٥٦٧٨٩٠0-9]+(?:\\s*[.\\-]|\\))(?!\\s*[١٢٣٤٥٦٧٨٩٠0-9])"
    is_boundary: true

  - id: POETRY_BLOCK
    description: >
      Classical Arabic poetry — at least two consecutive lines, each composed
      of two hemistichs separated by a star delimiter ` * `. Requiring two
      lines avoids firing on a stray asterisk in prose. The deterministic
      backbone for POETRY behavior (no LLM needed — the typographic shape
      itself carries the signal in this corpus).
    regex: "(?:[^*\\n]{10,}\\s\\*\\s[^*\\n]{10,}\\n){2,}"

  - id: SUBSECTION_MARKER
    description: >
      Author-typeset subsection marker — a line that begins with ``* `` (an
      asterisk followed by a space and content). In modern sīrah/tarīkh
      works the author uses ``* X :`` lines to delimit subsections within
      a chapter (e.g. ``* صدى الدعوة في يثرب :``, ``* تمهيد :``). 160 such
      lines appear in the target sīrah book; this is the dominant
      structural signal of the corpus and the boundary splitter must fire
      on each one. The line-start anchor (with MULTILINE) keeps poetry
      hemistich asterisks (mid-line ` * `) from matching.
    regex: "^\\*\\s+\\S"
    is_boundary: true

  - id: LETTER_ENTRY
    description: >
      Arabic-letter list item (أ - X, ب - Y, ج - Z, ...). Authors use abjad
      ordering for sub-lists nested inside numbered lists or as a parallel
      structure (e.g. analytical points within a subsection). The first ten
      abjad letters are: أ ب ج د ه و ز ح ط ي. Same boundary semantics as
      NUMBERED_ENTRY — each item is its own paragraph-level unit.
    regex: "^(?:أ|ب|ج|د|ه|و|ز|ح|ط|ي|ك)\\s*[\\-\\)]"
    is_boundary: true

  - id: ORDINAL_ENTRY
    description: >
      Arabic ordinal-word list marker (أولا, ثانيا, ثالثا, ...). Used in
      modern Arabic prose to enumerate analytical points or steps. Each
      ordinal at line-start with optional colon starts a new list item.
      Same boundary semantics as NUMBERED_ENTRY. Forms covered: the
      adverbial accusative (أولاً written as أولا after tashkeel strip).
    regex: "^\\s*(?:أولا|ثانيا|ثالثا|رابعا|خامسا|سادسا|سابعا|ثامنا|تاسعا|عاشرا|حاديا|ثانيا\\s+عشر|ثالثا\\s+عشر)\\s*[:\\-]?"
    is_boundary: true


# ─────────────────────────────────────────────────────────────────────────────
# BEHAVIORS
# Routing table: pattern combinations → behavior labels.
# Phase 2 consults this after detecting patterns on a span.
#
# Rules:
#   requires:     ALL listed patterns must be present
#   any_of:       AT LEAST ONE listed pattern must be present
#   none_of:      behavior does NOT fire if ANY of these patterns are present
#   genre_gate:   behavior only activates for these book_type values
#   priority:     higher number wins when multiple behaviors match (default: 0)
# ─────────────────────────────────────────────────────────────────────────────
behaviors:
  - id: HADITH_TRANSMISSION
    description: >
      A complete hadith unit — contains both the isnad (narrator chain) and the
      matn (prophetic text). Classification requires evidence of an actual chain
      AT THE START of the span: either ATTRIBUTION_STRONG_INITIAL (strong verb
      within the first ~20 chars) or ISNAD_BACK_REF (chain back-reference).

      The "INITIAL" qualifier is the key — modern sīrah authors cite hadiths
      mid-paragraph using ``روى ابن هشام`` / ``سمع البيهقي``, which makes plain
      ATTRIBUTION_STRONG fire deep inside narrative spans. That match indicates
      a *citation*, not the surrounding span being a hadith transmission. Only
      a chain marker at the start of the span (or after a numbered prefix)
      is a true hadith-chain indicator.

      MATN_BOUNDARY_HINT (قال رسول الله) is a matn-start marker, not a chain
      indicator — prose that mentions the Prophet fires it but has no chain.
    any_of: [ATTRIBUTION_STRONG_INITIAL, ISNAD_BACK_REF]
    # Secular-content genres carry no isnad hadith: their numbered catalogs,
    # transmission-verb grammar examples (أخبرنا as a conjugation), and cited
    # book titles otherwise route to HADITH and fabricate narrators. Blocking
    # the hadith rules there is the genre-axis complement to the per-genre gates
    # the fiqh/rijal/biography rules already carry. Anchored for reuse below.
    genre_block: &non_hadith_genres
      - arabic-language-sciences
      - medicine
      - logic-philosophy
      - poetry-collections
      - other-sciences
      - journals-miscellaneous
      - bibliographies-indexes
      - contemporary-islamic-issues
    priority: 80

  - id: HADITH_TRANSMISSION
    description: >
      عن-chain hadith — isnad starts with عن rather than an explicit verb.
      Common in hadith collections where the compiler omits their own chain
      prefix. Requires a genealogy marker (بن/ابن) to distinguish from
      prepositional عن ("about X"). Lower priority than the primary rule.
    requires: [AN_CHAIN_INITIAL, GENEALOGY_CHAIN]
    none_of: [HEADING_MARKER]
    genre_block: *non_hadith_genres
    priority: 78

  - id: HADITH_TRANSMISSION
    description: >
      Numbered hadith in compilatory collections (Bihar al-Anwar, al-Kafi,
      Wasāʾil, Muṣannaf, etc.). Format: ``N - Narrator1 بن X عن Narrator2...``

      Variant correctness relies on the ``start_threshold`` set on the
      ATTRIBUTION and GENEALOGY_CHAIN patterns: both must have their
      earliest match within the first ~100 chars of the span for the rule
      to count them. That is the hadith-chain shape — a name with بن /
      ابن and a transmission verb عن or حدثنا in the opening tokens.

      Without those thresholds the rule fired too cheaply: a modern sīrah
      content list like ``5 - أن الوفد الإسلامي...`` has prepositional عن
      and a cited "X بن Y" name somewhere deep in the entry, which is NOT
      a hadith chain. Thresholds restrict the routing-relevant matches to
      the entry-start region.
    requires: [NUMBERED_ENTRY, ATTRIBUTION, GENEALOGY_CHAIN]
    none_of: [HEADING_MARKER]
    genre_block: *non_hadith_genres
    priority: 72

  - id: HADITH_TRANSMISSION
    description: >
      Direct-report hadith — a numbered entry that reports from an authority
      without a relayed narrator chain: a question put to an Imam, or a
      ``source : matn`` citation. It carries a transmission verb عن / a
      genealogy link / a prophetic-speech hint but not the full chain shape the
      higher rules require, so without this rule it fell through to a bare
      NUMBERED_ENTRY and lost its hadith identity (and its own leaf node). Sits
      below the chain-based and genre-gated rules (rijal, biography, fiqh,
      historical), so those still claim their entries in their own genres; here
      it catches the numbered hadith that remain in hadith, tafsir, and theology
      volumes. Genre-blocked from secular content like every hadith rule.
    requires: [NUMBERED_ENTRY]
    any_of: [ATTRIBUTION, GENEALOGY_CHAIN, MATN_BOUNDARY_HINT]
    none_of: [HEADING_MARKER]
    genre_block: *non_hadith_genres
    priority: 69

  - id: QURAN_VERSE
    description: >
      Standalone Quranic text or verse reference. Does not fire when ATTRIBUTION
      is present — a span beginning with a hadith chain that cites a Quran verse
      as dalil is a HADITH_TRANSMISSION, not a QURAN_VERSE. BASMALA guard
      prevents the basmala (already handled by its own behavior) from triggering
      QURAN_VERSE when corpus matching detects it. MATN_BOUNDARY_HINT guard
      blocks long narrative prose that *cites* a verse from being classified
      as a verse — the hint matches "قال رسول الله / النبي" which never
      appears in pure Quranic text. AUTHOR_COMMENTARY guard keeps the author's
      exegesis (opening تفسير / بيان / أقول, which quotes قوله تعالى) as
      AUTHOR_COMMENTARY rather than absorbing the commentary into QURAN_VERSE.
    requires: [QURAN_REF]
    none_of: [ATTRIBUTION_STRONG, AN_CHAIN_INITIAL, BASMALA, MATN_BOUNDARY_HINT, AUTHOR_COMMENTARY]
    priority: 90

  - id: BIOGRAPHY
    description: Biographical entry about a scholar or person
    requires: [PERSON_REF]
    any_of: [DEATH_MARKER, BIRTH_MARKER, GENEALOGY_CHAIN]
    priority: 70

  - id: BIOGRAPHY
    description: >
      Scholar / narrator biography in narrative-genre books (sīrah, tarīkh,
      genealogy). A numbered entry that contains a hijri year AND at least
      one birth or death marker is unambiguously a biographical entry —
      even when ATTRIBUTION_STRONG also fires (because the bio describes
      the scholar's own transmission activities: ``روى عن فلان``,
      ``سمع من فلان``). Without this rule those spans falsely classify as
      HADITH_TRANSMISSION variant 1 (priority 80) — the cheap-pattern
      false positive the user flagged on ``1 - أبان بن عثمان :``.

      Priority 81 sits ABOVE HADITH variant 1 (80) so the BIRTH+DEATH+
      HIJRI_YEAR triplet outranks the strong-attribution heuristic. The
      genre gate keeps fiqh and hadith books (where ``ت 200`` style
      narrator-death inserts mid-chain are common in legitimate hadiths)
      unaffected — only narrative genres get this override.
    requires: [NUMBERED_ENTRY, HIJRI_YEAR]
    any_of: [BIRTH_MARKER, DEATH_MARKER]
    none_of: [HEADING_MARKER, BASMALA, POETRY_BLOCK]
    genre_gate: [prophet-imams-biography, genealogy-biography, history-geography]
    priority: 81

  - id: AUTHOR_COMMENTARY
    description: Author commentary or explanation (بيان, أقول)
    requires: [AUTHOR_COMMENTARY]
    priority: 85

  - id: FIQH_RULING
    description: >
      Legal ruling or juristic opinion. The trigger lexicon (يجب / يكره /
      يحل / يحرم) is polysemous: in kalam يكره means "compel" and يجب means
      "is necessary", so in theology, tafsir, and hadith volumes these words
      fire the rule spuriously and outrank NUMBERED_ENTRY on numbered hadith.
      The genre gate restricts the ruling behavior to fiqh books, the same way
      BIOGRAPHY and RIJAL_ENTRY gate their context-dependent rules; in a
      non-fiqh book a juristic-sounding entry stays a hadith or numbered entry.
    requires: [FIQH_RULING]
    genre_gate: [fiqh-terminology, hanafi-fiqh, hanbali-fiqh, independent-fiqh, maliki-fiqh, shafii-fiqh, shia-fiqh-8th-century, shia-fiqh-fatwas, shia-fiqh-pre-8th-century, shia-fiqh-principles, shia-hadith-fiqh, sunni-fiqh-principles, sunni-hadith-fiqh, zahiri-fiqh, zaidi-fiqh]
    priority: 75

  - id: HISTORICAL_NARRATIVE
    description: >
      Modern authorial historical narrative — the dominant content type in
      sīrah/tarīkh/biography books. Genre-gated AND requires positive cues
      (classical-source attribution, hijri year, or lunar month). The
      genre gate alone is not the guardrail — the positive-cue requirement
      is what prevents this behavior from absorbing every paragraph in a
      sīrah book that happens to fall through. Spans with NO positive cue
      remain GENERAL_PROSE. Priority sits ABOVE HADITH_TRANSMISSION
      variant 3 (72) so the cheap-pattern false positives we identified
      in long modern narrative reclassify here rather than as hadith, but
      BELOW variant 1 (80) and variant 2 (78) so real hadith chains in
      sīrah books (when they exist) keep their HADITH label.
    requires: []
    any_of: [CLASSICAL_SOURCE_ATTRIBUTION, HIJRI_YEAR, LUNAR_MONTH]
    none_of: [HEADING_MARKER, BASMALA, POETRY_BLOCK]
    genre_gate: [prophet-imams-biography, genealogy-biography, history-geography]
    priority: 73

  - id: SECTION_HEADING
    description: Structural heading (kitab, bab, fasl)
    requires: [HEADING_MARKER]
    priority: 95

  - id: SECTION_HEADING
    description: >
      Author-typeset subsection (line starts with ``* X :``). Same behavior
      label as the keyword-based heading rule above so downstream atomizers/
      extractors don't need to distinguish — both feed into the
      SECTION_HEADING bucket. Slightly lower priority so a span that has
      BOTH a kitab/bab heading AND a subsection marker still routes via
      the higher-level structural keyword.

      A subsection heading is a topic label (``* (الطينة والميثاق) *``), never a
      numbered entry: the NUMBERED_ENTRY guard keeps a numbered hadith that
      prints the same asterisk ornament (``* 39 - التوحيد : ابن الوليد ، عن ...``)
      from being filed as a chapter heading, which severed its isnad from its
      matn and hid the hadith. Without the ornament such a line already routes
      as a hadith; the guard makes the ornamented form behave the same.
    requires: [SUBSECTION_MARKER]
    none_of: [NUMBERED_ENTRY]
    priority: 92

  - id: RIJAL_ENTRY
    description: Rijal narrator biography entry in a narrator assessment book
    requires: [NUMBERED_ENTRY]
    any_of: [PERSON_REF, GENEALOGY_CHAIN, DEATH_MARKER]
    genre_gate: [sunni-hadith-narrators, shia-hadith-narrators]
    priority: 85

  - id: POETRY
    description: >
      Span where classical Arabic poetry (at least two consecutive
      star-delimited lines) is the strongest detected signal. Priority is
      deliberately LOW (65) — below HADITH_TRANSMISSION/AUTHOR_COMMENTARY/
      FIQH_RULING — because a hadith or fiqh discussion that quotes a couplet
      is not "poetry"; only a span whose strongest signal is the poetic shape
      gets the POETRY label. Pure-poetry spans (qasida quotations) fire here.
    requires: [POETRY_BLOCK]
    priority: 65

  - id: BASMALA
    description: Standalone basmala (بسم الله الرحمن الرحيم) — structural section opener
    requires: [BASMALA]
    priority: 50

  - id: NUMBERED_ENTRY
    description: Numbered list entry
    requires: [NUMBERED_ENTRY]
    priority: 60

  - id: NUMBERED_ENTRY
    description: >
      Arabic-letter list entry (أ - X, ب - Y, ج - Z, ...). Routed to the
      same NUMBERED_ENTRY behavior label so downstream atomizers and
      extractors don't need to distinguish — both are list items emitting
      ENTRY_UNIT. Lower priority than the digit-numbered variant.
    requires: [LETTER_ENTRY]
    priority: 58

  - id: NUMBERED_ENTRY
    description: >
      Arabic ordinal-word list entry (أولا :, ثانيا :, ثالثا :, ...).
      Routed to NUMBERED_ENTRY so downstream consumers don't fork.
    requires: [ORDINAL_ENTRY]
    priority: 57

  - id: GENERAL_PROSE
    description: Default behavior for content with no specific pattern match
    requires: []
    priority: 0

  - id: EDITORIAL_FRONTMATTER
    description: >
      Pages before the main content starts, as determined by TOC analysis.
      Assigned by content-boundary detection, not pattern routing.
    requires: []
    priority: -1


# ─────────────────────────────────────────────────────────────────────────────
# ATOMICIZERS
# How to split each behavior's spans into atomic units.
# Phase 3 looks up the behavior here.
# ─────────────────────────────────────────────────────────────────────────────
atomicizers:
  HADITH_TRANSMISSION:
    strategy: sanad_matn_split
    unit_types:
      isnad: ISNAD_UNIT
      matn: MATN_UNIT
    fallback_unit_type: HADITH_UNIT

  AUTHOR_COMMENTARY:
    strategy: whole_span
    unit_type: COMMENTARY_UNIT

  QURAN_VERSE:
    strategy: whole_span
    unit_type: QURAN_UNIT

  BIOGRAPHY:
    strategy: whole_span
    unit_type: BIOGRAPHY_UNIT

  FIQH_RULING:
    strategy: whole_span
    unit_type: FIQH_UNIT

  SECTION_HEADING:
    strategy: whole_span
    unit_type: HEADING_UNIT

  RIJAL_ENTRY:
    strategy: whole_span
    unit_type: RIJAL_UNIT

  NUMBERED_ENTRY:
    strategy: whole_span
    unit_type: ENTRY_UNIT

  BASMALA:
    strategy: whole_span
    unit_type: BASMALA_UNIT
    use_pattern_text: BASMALA

  POETRY:
    strategy: whole_span
    unit_type: POETRY_UNIT

  HISTORICAL_NARRATIVE:
    strategy: whole_span
    unit_type: HISTORICAL_NARRATIVE_UNIT

  GENERAL_PROSE:
    strategy: whole_span
    unit_type: PROSE_UNIT

  EDITORIAL_FRONTMATTER:
    strategy: whole_span
    unit_type: EDITORIAL_UNIT

  _special_unit_types:
    footnote: FOOTNOTE_UNIT


# ─────────────────────────────────────────────────────────────────────────────
# EXTRACTORS
# Which extractors to run per behavior.
# Phase 3 looks up behavior here to find which extractors to invoke.
# (Extractor implementations live in src/extractors/ — not yet built)
# ─────────────────────────────────────────────────────────────────────────────
extractors:
  HADITH_TRANSMISSION:
    - narrator_extractor
    - person_mention_extractor
    - date_expression_extractor
    - event_extractor
  AUTHOR_COMMENTARY:
    - grammar_term_extractor
    - date_expression_extractor
  QURAN_VERSE:
    - quran_entity_extractor
  BIOGRAPHY:
    - date_expression_extractor
    - event_extractor
  RIJAL_ENTRY:
    - narrator_extractor
    - date_expression_extractor
    - event_extractor
  FIQH_RULING: []
  SECTION_HEADING:
    - grammar_term_extractor
  BASMALA: []
  NUMBERED_ENTRY:
    - grammar_term_extractor
    - date_expression_extractor
    - event_extractor
  POETRY: []
  GENERAL_PROSE:
    - grammar_term_extractor
    - date_expression_extractor
    - event_extractor
  HISTORICAL_NARRATIVE:
    - date_expression_extractor
    - event_extractor
  EDITORIAL_FRONTMATTER: []

thresholds:
  min_span_chars: 20
  narrative_heading_max_chars: 300
  heading_split_max_heading_chars: 150
  isnad_chain_proximity_max: 40
  isnad_chain_gap_max: 85
  narrator_name_max_chars: 100
  question_verb_lookback_chars: 60
  isnad_tail_max_chars: 120
  isnad_merge_name_max_chars: 60
  heading_split_min_heading_chars: 8
  evidence_context_chars: 50
  narrator_disqualifier_lookahead_chars: 20
  narrator_narrative_lookahead_chars: 15


# ─────────────────────────────────────────────────────────────────────────────
# TOC SECTIONS
# Content-boundary detection: which existing patterns identify the start of
# main content in TOC entry titles. Phase 2 tests each TOC title against
# these patterns (in order). The first match marks the content-start page.
# All pages before that page get behavior EDITORIAL_FRONTMATTER.
# ─────────────────────────────────────────────────────────────────────────────
toc_sections:
  # Patterns tested against TOC entry titles, in order. The first TOC entry
  # whose cleaned title matches ANY pattern marks the page where main content
  # begins; pages before that are EDITORIAL_FRONTMATTER. Lower-priority
  # patterns help catch books whose first real chapter title doesn't contain
  # a classical heading keyword — e.g. modern sīrah works that use ``* X :``
  # subsection markers as the primary structural unit.
  content_start_patterns: [HEADING_MARKER, SUBSECTION_MARKER]


# ─────────────────────────────────────────────────────────────────────────────
# FAILURE BUDGET
# The operational form of CLAUDE.md's "fail loudly" — a hard halt threshold
# for silent fallbacks. When the share of content spans that fall through to
# GENERAL_PROSE WITHOUT matching any explicit routing rule exceeds this
# percentage, Phase 2 raises SegmentError. This catches the failure mode the
# council identified: a routing taxonomy that has silently stopped modelling
# its corpus, producing structurally valid garbage. Tightening this number is
# how we ratchet quality up; loosening it should require a written justification.
# ─────────────────────────────────────────────────────────────────────────────
failure_budget:
  # Initial ceiling chosen just above the highest currently-observed drift
  # post-TOC track (sira at ~16.6%). The number is a ratchet — every PR
  # that adds a behavior rule for an observed pattern combo should lower
  # this. Target milestones: 15% (post-routing-coverage pass), 10%
  # (post-HISTORICAL_NARRATIVE expansion), 5% (production target). The
  # intent is for this value to fall over time, not rise; raising it
  # requires written justification in PR.
  unclassified_routed_max_pct: 20.0
  min_content_spans_for_enforcement: 20
narrative_genres:
  # book_types where the modern-authorial-narrative routing applies. Used by
  # Phase 2 to gate the HEADING_MARKER / FIQH_RULING drop for long spans
  # carrying narrative evidence (classical-source attribution, hijri date,
  # lunar month). Fiqh and hadith book_types are intentionally NOT here:
  # a fiqh manual that mentions a date in a ruling should keep its FIQH
  # label, not get reclassified as narrative.
  book_types:
    - prophet-imams-biography
    - genealogy-biography
    - history-geography


grammar_extraction:
  # Book categories whose spans the grammar_term_extractor scans for iʿrāb /
  # naḥw / ṣarf markers. Scoped so the term regexes (نصب, جر, حذف...) read as
  # their grammatical senses, which only holds in a grammar/language work.
  genres:
    - arabic-language-sciences


cross_page_repair:
  name_prefix_tokens:
    - عبد
    - أمة
    - عبيد
    - أبو
    - أبي
    - أم
    - ابن
    - بن
    - بنت
    - أهل
    - بني
    - آل
    - ذو
    - ذي


# ─────────────────────────────────────────────────────────────────────────────
# NARRATOR EXTRACTION
# Domain-specific settings for hadith narrator extraction in Phase 3.
# ─────────────────────────────────────────────────────────────────────────────
narrator_extraction:
  relative_references:
    - "أبيه"
    - "أبيهما"
    - "أبوه"
    - "أبوهما"
    - "أمه"
    - "أمها"
    - "جده"
    - "جدته"
    - "عمه"
    - "عمته"
    - "أخيه"
    - "أخته"
    - "خاله"
    - "خالته"
    - "ابنه"
    - "ابنته"

  # First-person kinship references a compiler uses for his own teacher, e.g.
  # al-Saduq's "أبي ، عن سعد". Unlike the third-person forms above these are
  # matched ONLY when they stand alone as the whole name candidate, never as a
  # prefix: "أبي عبد الله" / "ابن أبي عمير" are kunyas where أبي is part of the
  # name, so a prefix match would wrongly split a person. Standalone bare "أبي"
  # is "my father" and emits as a relative_reference like bare "أبيه".
  first_person_references:
    - "أبي"
    - "جدي"
    - "عمي"
    - "أخي"
    - "خالي"
    - "أمي"

  prepositional_an_exclusions:
    - "يمين"
    - "شمال"
    - "يسار"
    - "رأس"
    - "ظهر"
    - "جنب"
    - "وجه"
    - "فرس"
    - "سبيل"
    - "طريق"
    - "قلب"
    - "ذلك"
    - "هذا"
    - "هؤلاء"
    - "المدينة"
    - "الدنيا"
    - "الآخرة"
    - "نفسه"
    - "نفسها"
    - "نفسي"

  narrative_context_words:
    - "في"
    - "ذلك"
    - "هذا"
    - "شيئ"

  # Words after أنا that indicate it is the pronoun "I" (أنا أسمع = I was
  # listening), not the transmission abbreviation أنا = أخبرنا.
  pronoun_ana_exclusions:
    - "أسمع"
    - "أعلم"
    - "أعرف"
    - "أكتب"
    - "أقرأ"
    - "أنظر"
    - "أحفظ"
    - "أحسب"
    - "قائم"
    - "حاضر"
    - "معه"
    - "معهم"
    - "معها"
    - "شاهد"

  # Words that are never standalone narrator names (pronouns, common words)
  narrator_stopwords:
    - "هو"
    - "هي"
    - "هم"
    - "هن"
    - "هكذا"
    - "غيرهم"
    - "الحديث"
    - "فتى"
    - "يقول"
    - "عنه"
    - "عنها"
    - "ابن آدم"

  # First words that never head a person name: bibliographic nouns (كتاب X,
  # خبر X, كلام له) and formulaic isnad-grade terms (في الصحيح / الموثق). A
  # narrator or mention candidate whose first token is one of these is a
  # citation or grading phrase that leaked past the citation-head boundary, not
  # a name, so it is dropped. الحسن is deliberately excluded: it is also a very
  # common given name (الحسن بن علي).
  non_name_leading_words:
    - "كتاب"
    - "خبر"
    - "كلام"
    - "باب"
    - "الصحيح"
    - "الموثق"

  # Collective co-narrator phrases: "and others / more than one", closing a list
  # of named narrators (صباح ، وهشام وحفص وغير واحد). They are not persons, and
  # the internal waw of واحد would mis-split, so they are removed as a whole unit
  # before the conjunction split runs.
  co_narrator_collectives:
    - "غير واحد"

  transmission_tail_verbs:
    - "يحدث"
    - "يروي"
    - "يخبر"

  name_content_boundaries:
    # Patterns that mark where a narrator name ends and content begins
    an_clause_not_transmission: "\\bأنه[ام]?\\s+(?!سمع|حدث|أخبر|روى|ثنا|نبأ)"
    book_abbreviation_ref: "\\b(?:يب|كا|ئل|فقيه)\\s+\\d"
    topical_fi: "\\bفي\\s+(?:حديث|كتاب|باب|قول|رواية|تفسير|سورة|يب|كا|ئل|مسجد|مكة|المدينة|نار|عصمت)"
    nahw_formula: "\\bنحو\\s+ما\\b"
    mithluhu: "\\bمثله\\b"
    editorial_reference: "\\bوقد\\s+أوردنا"
    question_mark: "[؟?]"
    qala_colon: "\\bقال\\s*:\\s*\\S"
    an_not_name: "\\b[اأإ]ن\\s+(?!يحيى|يزيد|يعقوب|يوسف|يونس)\\S+\\s"
    fa_narrative_verbs: "\\bف(?:هل|لم|لما|إذا|قام|جاء|أخذ|دخل|خرج|قال|سأل)\\b"
    thumma_inna: "\\bثم\\s+إن\\b"
    vocative: "\\bيا\\s+"
    colon_separator: "\\s+:\\s+"
    saqita: "\\bساقطة\\b"
    kanat_prophet: "\\bكانت?\\s+(?:رسول|النبي|تحت)"
    # Transmission method phrases (صيغ الأداء) — follow the narrator name,
    # never part of it.  E.g. "الصواف قراءة عليه وأنا أسمع قال"
    transmission_method: "(?:قراءة|قرئ|عرض(?:ت|نا)?|سماعا?)\\s+(?:عليه|عليها|علينا|عليهم|من\\b)"
    audition_witness: "وأنا أسمع"
    # Honorific formulas that follow a person's name, never part of it.
    honorific_salawat: "\\bصلى\\s+الله\\s+عليه"
    honorific_praise: "\\bرضي\\s+الله\\s+عنه"
    # Demonstrative + content noun: "هذا الحديث" (this hadith) is content, not a name.
    demonstrative_content: "(?:هذا|هذه|بهذا|بهذه|لهذا|لهذه)\\s+(?:الحديث|الخبر|الأثر|القول|الرواية|الكتاب|الباب)"
    honorific_mercy: "\\bرحم[ةه]\\s+الله\\b"
    # Cross-reference verbs with pronoun suffixes — "رواه البيهقي" (he narrated it)
    # is a reference to another scholar's collection, never a narrator name.
    cross_reference_verb: "\\bو?(?:رواه[ا]?|أخرجه[ا]?|ذكره[ا]?|هكذا)\\b"
    # Pronoun "he/she" at start of name_slice is always commentary, never a name.
    pronoun_sentence_start: "^\\s*ه[وي]\\s"
    # Present-tense transmission verbs after a narrator name end the name.
    # Includes pronominal suffix forms (يحدثك, يحدثه, يرويه, etc.)
    transmission_tail: "\\b(?:يحدث|يروي|يخبر|يذكر)(?:ه[امن]?|ك[مـ]?|نا|ون)?\\b"
    # "كان من / كانت من" — biographical phrase ("was from the companions of..."),
    # never part of a narrator name.
    kana_min: "\\bكان[ت]?\\s+من\\b"
    # Narrator grading vocabulary — "ثقة" (trustworthy), "ضعيف" (weak), etc.
    # These follow a name as evaluation, never part of it.
    grading_phrase: "\\b(?:ثقة|ضعيف|كذاب|قليل الحديث|كثير الحديث|مجهول)\\b"
    # "وهو/وهي" + descriptive verb/adjective — circumstantial clause describing
    # state, never part of a narrator name.
    waw_huwa_descriptive: "\\bوه[وي]\\s+(?:يصلي|ينظر|جالس|قائم|واقف|متكئ|راكب|نائم|قاعد|مضطجع|من\\s+مشا[يئ]خ|مستقبل)"
    # "الحديث" at the end of a name_slice (with optional ellipsis/dots) is a
    # cross-reference, not part of the narrator name.
    trailing_hadith: "\\.\\s*\\.?\\s*\\.?\\s*الحديث|\\bالحديث\\s*\\d|\\bالحديث\\s*$"
    # "قد تقدم" (has preceded) — editorial cross-reference phrase.
    qad_taqaddama: "\\bقد\\s+تقدم\\b"
    # "إنه/إنها" (indeed he/she) — introduces content clause, not a name.
    # Distinguished from أنه which has its own boundary with transmission lookahead.
    innahu_content: "\\b[إا]نه[ام]?\\s+(?!سمع|حدث|أخبر|روى)"
    # "لأنه/لأنها" (because he/she) — causal clause.
    li_annahu: "\\bلأنه[ام]?\\s"
    # "أنه سمع" mid-name merges two narrators — treat as boundary.
    # Distinct from an_clause_not_transmission which has a lookahead that
    # allows "أنه سمع" as transmission. When it appears inside a name_slice
    # (between two attribution verbs), it signals a narrator boundary.
    anahu_samiʿa: "\\bأنه[ام]?\\s+سمع"
    # Reference numbers: ") . 6243" or similar — editorial apparatus.
    reference_number: "[)\\]]\\s*\\.?\\s*[-–]|[)\\]]\\s*\\.?\\s*\\d{3,}"
    # Poetry marker: * between hemistiches (with surrounding whitespace).
    poetry_marker: "\\s\\*\\s"
    # "كما" (as / just as) — introduces comparison or attestation clause.
    kama_clause: "\\bكما\\s+(?:صرح|يظهر|ذكر|قال|روى|نقل|تقدم|في\\b)"
    # "باسناده إلى" (with his chain to) — transmission phrase in commentary.
    bi_isnadihi: "\\bباسناده[ام]?\\s+(?:إلى|الى|عن)\\b"
    # "بقراءتي / بقراءته" (by my/his reading) — audition method phrase.
    bi_qiraati: "\\bبقراءت[يه]\\b"
    # "فكأن" (as if) — commentary/editorial phrase.
    fa_kaanna: "\\bفكأن\\b"
    # "صلوات الله عليه" — Shia honorific formula, not part of name.
    honorific_salawat_shia: "\\bصلوات\\s+الله\\s+علي[هـ]"
    # "قدس الله روحه" — honorific for deceased scholar.
    honorific_quds: "\\bقدس\\s+(?:الله\\s+)?(?:روحه|سره)"
    # "إذا / إذ" — temporal/conditional clause start.
    idha_clause: "\\bإذ(?:ا)?\\s+(?:لا|لم|كان|جاء|دخل|قام)"
    # Dictionary definition patterns — "إذا جمعه" etc.
    dictionary_def: "\\bإذا\\s+(?:جمعه|فعله|قاله|ضربه)"
    # Persian text detection — Persian function words and verb endings.
    # Catches Persian content leaking into narrator names from bilingual books.
    persian_content: "\\sرا\\s|\\s(?:است|بود|شد[ه]?|شود|كرد|گفت(?:ند)?|آمد|نيست|باشد|نمايد|داشت[ه]?)\\b|\\bمى\\S|\\bنمى\\S|\\b(?:كه|چون|چنانچه|اگر|ولى|زيرا|بلكه|براى|سپس)\\s|\\bسخن\\s"
    # "لكن" (but/however) — introduces adversative clause, not a name.
    lakin_clause: "\\bلكن\\s"
    # "إنما" (only/indeed) — introduces emphatic clause.
    innama_clause: "\\bإنما\\s"
    # "انتهى" (end of quote) — editorial phrase marking end of citation.
    intaha: "\\bانتهى\\b"
    # "أن هذا / ان هذا" (that this) — content clause.
    an_hadha: "\\b[اأإ]ن\\s+هذ[اه]\\b"
    # "عليه السلام" honorific when followed by content clause.
    # This catches "الصادق عليه السلام ان ..." where the post-honorific content leaks.
    alayhis_salam_content: "\\bعليه[ام]?\\s+السلام\\s+(?:أن|ان|إن|انه|أنه)"
    # «guillemets» — editorial quotation marks signal named-entity boundaries.
    guillemet: "»"
    # "الذي/التي" (who/which) — relative clause, not part of name.
    relative_clause: "\\bالذي\\s|\\bالتي\\s"
    # "من جهة" (from the side of) — biographical/genealogical phrase.
    min_jiha: "\\bمن\\s+جهة\\b"
    # Folio/page markers in editorial brackets: (/ أ [4 / أ])
    folio_marker: "\\(/\\s*[أب]"
    # Parenthetical reference codes: (ح 98), (خ 87), (ك 149), (ر 31)
    paren_reference: "\\(\\s*[حخكر]\\s+\\d"
    # "فإن" (for indeed) — introduces causal/explanatory clause.
    fa_inna: "\\bفإن\\s"
    # "فاذهب / فاطلب" — imperative after fa-, not a name.
    fa_imperative: "\\bفا(?:ذهب|طلب|تق|سأل|نظر)\\b"
    # "لئن" (if indeed) — conditional clause start (common in sermons/Quran).
    la_in: "\\bلئن\\s"
    # "نقله" (he transmitted it) — cross-reference verb with pronoun suffix.
    naqalahu: "\\bنقله[ا]?\\b"
    # "ما خلا" (except for) — exclusion clause.
    ma_khala: "\\bما\\s+خلا\\b"
    # "يناقض / يقوم / يلزم" — present-tense verbs as content boundary.
    present_tense_content: "\\b(?:يناقض|يلزم|يقاس|يتوكأ)\\b"
    # "وبناء على" (and based on) — editorial/analytical phrase.
    bina_ala: "\\bوبناء\\s+على\\b"
    # "العبارة" (the phrase) — editorial/commentary reference.
    al_ibara: "\\bالعبارة\\b"
    # "إلى أصنام / إلى + non-name noun" — directional content.
    ila_content: "\\bإلى\\s+(?:أصنام|بيت|دار|أرض|مكان|بلاد|جبل)"
    # "فذكره / فذكرها" — fa- + cross-reference verb.
    fa_dhakara: "\\bف(?:ذكره[ا]?|رواه[ا]?|أخرجه[ا]?)\\b"
    # "جميعا" (together) — adverb after a name list, not a name.
    jamian: "\\bجميعا\\b"
    # "ليس" (is not) — negation clause.
    laysa: "\\bليس\\b"
    # "وقد" (and indeed) — editorial/narrative clause starter.
    wa_qad: "\\bوقد\\s"
    # "صحبت / صحبنا" (I accompanied / we accompanied) — content verb.
    sahibtu: "\\bصحب[تن]"
    # "تكلموا / تكلمت" (they/she spoke) — content verb.
    takallamu: "\\bتكلم[وت]"
    # "فهذه / فهذا / فهؤلاء" — demonstrative with fa-.
    fa_demonstrative: "\\bف(?:هذ[اهي]|هؤلاء)\\s"
    # "[في" — editorial bracket with preposition.
    bracket_content: "\\[في\\s"
    # "انا ابن" (I am the son of) — pronoun I + genealogy.
    ana_ibn: "\\b[اأ]نا\\s+(?:ابن|بنت)\\b"
    # "فليقل / فليشهد" — jussive with fa-li-.
    fa_li_jussive: "\\bفل(?:يقل|يشهد|يقم|ينظر|يأخذ|يصل)\\b"
    # "فكفى" (for sufficient is) — emphatic clause.
    fa_kafa: "\\bفكفى\\b"
    # "واكظم / واقصر / واسكن / واحذر / واصفح" — imperative with waw.
    wa_imperative: "\\bوا(?:كظم|قصر|سكن|حذر|صفح|كثر|ستصلح)\\b"
    # "فمن" (so whoever) — conditional with fa-.
    fa_man: "\\bفمن\\s"
    # "عده الشيخ" (the sheikh counted him) — biographical evaluation.
    addahu: "\\bعده\\s"
    # "فيقضون / فيقولون" — present tense with fa-.
    fa_present: "\\bفيقض|\\bفيقول|\\bفيأت"
    # "القضية في" (the case/incident in) — narrative content.
    al_qadiyya: "\\bالقضية\\s"
    # "يرفعون / يسندون" (they raise/attribute) — group verb content.
    group_verb: "\\b(?:يرفعون|يسندون|يحتجون|يستدلون)\\b"
    # "أدام الله / أيد الله" — prayer formula after scholar name.
    prayer_formula: "\\b(?:أدام|أيد|أعز|حفظ|نصر)\\s+الله\\b"
    # "لم يثنوا / لم ينتضل" — negated past with لم.
    lam_verb: "\\bلم\\s+ي"
    # "قد اتخذوا" — perfect with قد + plural.
    qad_verb: "\\bقد\\s+(?:اتخذ|استقر|علم|ثبت)"
    # "قائم أصلي" — present tense prayer/action after state.
    state_action: "\\bقائم[اة]?\\s+أصلي\\b"
    # "ألا تسمعون" (do you not hear) — rhetorical question.
    ala_tasmaun: "\\bألا\\s+[تي]"
    # "فإنما / فإنها" — emphatic explanatory clause.
    fa_innama: "\\bفإن(?:ما|ها|هم|ه)\\s"
    # "ما أنكروا" (they did not deny) — negated past.
    ma_past: "\\bما\\s+أنكر"
    # "نستعين" (we seek help) — liturgical/prayer phrase.
    nastaeen: "\\bنستعين\\b"
    # "لا في" (not in) — negation + preposition content.
    la_fi: "\\bلا\\s+في\\s"
    # "(ع)" Shia honorific abbreviation followed by content.
    honorific_ayn_content: "\\(ع[\\).\\s]\\s*(?:ي|ل|ف|و(?!عن\\b))"
    # "معلوما أن" — known-that clause.
    maʿluman: "\\bمعلوما?\\s+[اأإ]ن\\b"
    # Names starting with ya that are NOT content boundaries
    ya_names_not_boundary: ["يحيى", "يزيد", "يعقوب", "يوسف", "يونس"]

  sentence_start_disqualifiers:
    - "قد"
    - "أما"
    - "كما"
    - "هناك"
    - "من المعلوم"
    - "سقطت"
    - "أدخل"
    - "فات"
    - "وقد"
    - "لأن"
    - "قلت"
    - "سمع"
    - "هنا"
    - "فإن"
    - "كان"
    - "هذا"
    - "ذكر"
    - "وهذ"
    - "ولكن"
    - "ونظرا"
    - "ومنها"
    - "وقال"
    - "قال"
    - "من"

# ---------------------------------------------------------------------------
# QURAN EXTRACTION
# The Quran manifestation genre label and named-entity gazetteer. The extractor
# (pipeline/extractors/quran_entities.py) runs only on spans whose book_type is
# in genres, and matches each verse against gazetteer names. A name that also
# reads as a common word carries a preceding-word rule validated against every
# Quranic occurrence: entity_prev keeps it only after a listed word, common_prev
# drops it after a listed word.
# ---------------------------------------------------------------------------
quran_extraction:
  genres:
  - quran
  gazetteer:
    prophet:
    - name: آدم
    - name: إدريس
    - name: نوح
    - name: هود
      entity_prev:
      - أخاهم
      - أخوهم
      - يا
      - قوم
      - نجينا
    - name: صالح
      entity_prev:
      - أخاهم
      - أخوهم
      - يا
      - قوم
      - نجينا
      - أن
    - name: إبراهيم
    - name: لوط
    - name: إسماعيل
    - name: إسحاق
    - name: يعقوب
    - name: يوسف
    - name: أيوب
    - name: شعيب
    - name: موسى
    - name: هارون
    - name: ذو الكفل
    - name: داود
    - name: سليمان
    - name: إلياس
    - name: اليسع
    - name: يونس
    - name: زكريا
    - name: يحيى
    - name: عيسى
    - name: محمد
    - name: أحمد
    person:
    - name: فرعون
    - name: هامان
    - name: قارون
    - name: جالوت
    - name: طالوت
    - name: آزر
    - name: مريم
    - name: لقمان
    - name: ذو القرنين
    - name: عزير
    - name: العزيز
      entity_prev:
      - امرأت
      - أيها
    - name: أبي لهب
    angel:
    - name: جبريل
    - name: ميكال
    - name: هاروت
    - name: ماروت
    - name: مالك
      entity_prev:
      - يا
    people:
    - name: عاد
      common_prev:
      - لا
      - من
      - حتى
    - name: ثمود
    - name: مدين
    - name: بني إسرائيل
    - name: قريش
    - name: الروم
    - name: يأجوج
    - name: مأجوج
    - name: أصحاب الكهف
    - name: سبأ
    - name: تبع
      entity_prev:
      - قوم
    place:
    - name: مكة
    - name: بكة
    - name: مصر
    - name: الطور
    - name: سينين
    - name: سيناء
    - name: بابل
    - name: الأحقاف
    - name: إرم
    - name: بدر
    - name: حنين
    - name: المسجد الحرام
    - name: المسجد الأقصى
    scripture:
    - name: القرآن
    - name: التوراة
    - name: الإنجيل
    - name: الزبور
    idol:
    - name: اللات
    - name: العزى
    - name: مناة
    - name: ود
      entity_prev:
      - تذرنّ
    - name: سواع
    - name: يغوث
    - name: يعوق
    - name: نسر
    - name: بعل

# ---------------------------------------------------------------------------
# EVENT EXTRACTION
# Historical-event genre gate, participation markers, and named-event
# gazetteer (by type) for pipeline/extractors/events.py. An event name that
# also reads as a common word carries a preceding-word rule (entity_prev).
# ---------------------------------------------------------------------------
event_extraction:
  genres:
  - history-geography
  - prophet-imams-biography
  - genealogy-biography
  - shia-hadith-general
  - sunni-hadith-general
  - shia-hadith-narrators
  - sunni-hadith-narrators
  - sunni-hadith-fiqh
  - shia-hadith-fiqh
  - shia-tafsir
  - sunni-tafsir
  - sects-schools
  participation:
    shahid:
    - شهد
    - شهدت
    - حضر
    - حضرت
    qutila:
    - قتل
    - استشهد
    - قُتل
    participant:
    - شارك
    - قاتل
  gazetteer:
    BATTLE:
    - name: بدر
      event_id: badr
      entity_prev:
      - يوم
      - غزوة
      - وقعة
      - معركة
      - أصحاب
      - أهل
      - شهد
    - name: أحد
      event_id: uhud
      entity_prev:
      - يوم
      - غزوة
      - وقعة
      - معركة
      - أصحاب
    - name: حنين
      event_id: hunayn
      entity_prev:
      - يوم
      - غزوة
      - وقعة
      - معركة
    - name: الخندق
      event_id: khandaq
      entity_prev:
      - يوم
      - غزوة
      - وقعة
      - معركة
    - name: الأحزاب
      event_id: ahzab
      entity_prev:
      - يوم
      - غزوة
      - وقعة
      - معركة
    - name: خيبر
      event_id: khaybar
    - name: مؤتة
      event_id: mutah
    - name: تبوك
      event_id: tabuk
    - name: بني قريظة
      event_id: qurayza
    - name: بني النضير
      event_id: nadir
    - name: بني المصطلق
      event_id: mustaliq
    - name: ذات الرقاع
      event_id: dhat_al_riqa
    - name: الجمل
      event_id: jamal
      entity_prev:
      - يوم
      - وقعة
      - معركة
      - حرب
    - name: صفين
      event_id: siffin
    - name: النهروان
      event_id: nahrawan
    - name: الحرة
      event_id: harra
      entity_prev:
      - يوم
      - وقعة
      - معركة
    - name: القادسية
      event_id: qadisiyya
    - name: اليرموك
      event_id: yarmuk
    - name: نهاوند
      event_id: nahawand
    - name: الطف
      event_id: taff
      entity_prev:
      - يوم
      - وقعة
      - معركة
    CONQUEST:
    - name: فتح مكة
      event_id: fath_makka
    TREATY:
    - name: الحديبية
      event_id: hudaybiyya
      entity_prev:
      - صلح
      - يوم
      - عام
      - غزوة
      - وقعة
    MARTYRDOM:
    - name: كربلاء
      event_id: karbala
    ASSEMBLY:
    - name: السقيفة
      event_id: saqifa
    - name: الغدير
      event_id: ghadir
      entity_prev:
      - يوم
      - غدير
      - خم
