node_modules
build
dist

# Per-machine local Claude Code settings + transient harness output: not
# project-owned source. settings.local.json is regenerated as permissions change.
.claude/settings.local.json
.claude/state/**
.claude/audit/**
.venv
.pnpm-store
coverage
playwright-report
test-results
**/*.lock
**/*.lockb
pnpm-lock.yaml
package-lock.json

# ADRs are historical snapshots where layout intent matters
# (e.g. metadata-line breaks, identifier underscores like text_en that
# prettier ambiguously interprets as italic). Excluded from autofix.
docs/adr/**

# Council reviews and handoff notes are historical snapshots too (transcripts,
# point-in-time HTML reports). Same rationale as ADRs — excluded from autofix.
docs/councils/**
docs/handoff/**

# Generated index of the upstream book corpus — produced by
# scripts/ingest_books.py. Not hand-edited; formatting is irrelevant.
data/books_index.json

# Per-verse CAMeL morphology feeding the Qurʾān extraction — captured from the
# morphology service, not hand-edited. Formatting is irrelevant.
data/quran_morphology.json

# Qurʾān verse text, vendored verbatim from the upstream sol-next pipeline
# (data/quran/quran_clearquran.json). Not hand-edited; formatting is irrelevant.
data/quran.json
# data/toc_overrides.json: 344k-line hand-curated TOC override map for the hadith
# corpus. Not machine-formatted; prettier would churn every line on each edit.
data/toc_overrides.json

# Ported sol-next pipeline config — vendored verbatim from the sibling sol-next
# project. Not hand-edited, so its formatting is irrelevant and prettier must
# not rewrite it.
config/sol.yaml

# Vendored/shared agent skills, symlinked in from the global skills library.
# They are not project-owned source; don't reformat them.
.agents/skills/**
